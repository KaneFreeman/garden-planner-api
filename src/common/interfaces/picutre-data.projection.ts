export interface PictureDataProjection {
  readonly _id: string;
  readonly date: Date;
  readonly id: number;
  readonly pictureId: string;
  readonly thumbnail: string;
  readonly deleted: boolean;
}
